<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Login - Moovox</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">

  <!-- Bootstrap e ícones -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css" rel="stylesheet">

  <!-- Fonte personalizada -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #6d960e;
      background-image: url('https://www.transparenttextures.com/patterns/football-no-lines.png'); /* textura de campo */
      background-size: cover;
      background-position: center;
      backdrop-filter: blur(2px);
    }

    .login-card {
      background-color: rgba(255, 255, 255, 0.95);
      border-radius: 1.5rem;
      box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      border: 2px solid #a5d6a7;
    }

    .logo {
      height: 150px;
      object-fit: contain;
      margin-bottom: 1rem;
    }

    .form-control {
      border-radius: 0.75rem;
    }

    .btn-primary {
      background-color: #4caf50;
      border: none;
      border-radius: 0.75rem;
      transition: all 0.3s ease;
      font-weight: 600;
    }

    .btn-primary:hover {
      background-color: #388e3c;
    }

    .text-green {
      color: #388e3c;
    }

    a {
      color: #4caf50;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .input-group-text {
      background-color: #f1f8e9;
    }
  </style>
</head>
<body>
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="login-card" style="width: 100%; max-width: 420px;">
      <div class="text-center">
        <img src="/images/logo-moovox-redimensionado.png" alt="Logo Moovox" class="logo">
        <h3 class="text-green">Bem-vindo ao Moovox</h3>
        <p class="text-muted mb-4">Gestão inteligente de animais no campo</p>
      </div>

      <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="email" class="form-label">E-mail</label>
          <div class="input-group">
            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
            <input type="email" class="form-control" id="email" name="email" required autofocus autocomplete="off">
          </div>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Senha</label>
          <div class="input-group">
            <span class="input-group-text"><i class="bi bi-lock"></i></span>
            <input type="password" class="form-control" id="password" name="password" required>
          </div>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-primary">Entrar</button>
        </div>

        <div class="mt-3 text-center">
          <a href="/recuperar-senha">Esqueci minha senha</a>
        </div>
      </form>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
